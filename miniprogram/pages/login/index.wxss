button{
      margin-top: 50rpx;
      width: 60%;
      border-radius: 20px 20px 20px 20px;
      background-color:var(--themeColor);
      border-color: var(--themeColor);
      color: white;
      font-size: 32rpx;
}